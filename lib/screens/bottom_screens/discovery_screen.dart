import 'package:flutter/material.dart';

class DiscoveryScreen extends StatelessWidget {
  DiscoveryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hello from discovery'),
      ),
    );
  }
}
